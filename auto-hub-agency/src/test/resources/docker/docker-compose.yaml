services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=auto_hub_agency'
      - 'POSTGRES_USER=admin'
      - 'POSTGRES_PASSWORD=password'
    ports:
      - '5432:5432'
